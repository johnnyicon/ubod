# Minimal Claude setup

1. Open Claude → Settings → Custom instructions.
2. Paste the content from `system-prompt-template.md` and replace the placeholders.
3. For each task, paste any relevant instruction files (from your monorepo) before asking Claude to plan.
